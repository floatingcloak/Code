#include <cstdio>
#include <cstring>
#include <iostream>

using namespace std;

struct Splay{int }

void NewNode(int n,int f,Splay *Fa){
    Node *P = 
}

void Build(int l,int r,int f){
    Kong = NewNode(0,0,Null);Head = NowNode(0,0,NULL);
    Sum += 2;Head -> S = Sum;Splay *Now = Head;int t = 1;Sum--;
    while(Sum--){
        Now -> Ch[1] = NewNode(t++,1,Now);Now = Now -> Ch[1];Now -> S = n + 1;
        if(rand() % 2)splay(Now,Kong);
    }return;
}

int main(){
    scanf("%d%d",&n,&m);Build(n);

}