#include <cstdio>
#include <cstring>
#include <iostream>

using namespace std;



int main() {
	scanf("%s", (s + 1));
	int n = strlen(s + 1);
	for(int i = 1; i <= n; ++i) {
		for(int j = 1; j <= n; ++j) {
			for(int k = j + 1; k <= n; ++k) {
				if(Judge(j, k) == 1) {
					
				}
			}
		}
	}
}
